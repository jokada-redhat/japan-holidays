[project]
name = "japan-holidays"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pyyaml"]

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
markers = [
    "network: tests that require network access to external servers",
]
